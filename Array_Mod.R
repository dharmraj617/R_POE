# Create an array with three columns, three rows, and two "tables" using two vectors
vector1 <- c(1, 2, 3)
vector2 <- c(4, 5, 6)
array_3d <- array(c(vector1, vector2), dim = c(3, 3, 2))

# Print the array
print(array_3d)
